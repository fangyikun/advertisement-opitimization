# 灵犀后端 .env 示例
# 复制到 /var/www/lingxi/backend/.env 并填写实际值

# 数据库（阿里云可用自建 MySQL 或 RDS）
DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=root
DB_PASSWORD=你的数据库密码
DB_NAME=sign_inspire

# 天气 API（Open-Meteo 免费无需 key，可选）
# OPENWEATHER_API_KEY=

# Google Places（门店推荐带图片时需要）
GOOGLE_PLACES_API_KEY=你的Key

# OpenAI / 大模型（自然语言解析规则时需要）
OPENAI_API_KEY=你的Key
